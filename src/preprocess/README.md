# Preprocessing

1. Extract json files from raw html scrapped.
    - output: data files in `data/`
2. Extract from `data/reviews.json` via `scripts/00_preprocess/analyse_reviews.py` review tags.
    - output: `data/review_tags.json`